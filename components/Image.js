import styled from 'styled-components';
import { space, flex, size, border, color } from '../utils/styled';

const Image = styled.Image`
  ${space}
  ${flex}
  ${size}
  ${border}
  ${color}
`;

export default Image;
